version: "3.8"
services:
  jellybot:
    build: .
    container_name: jellycorrd
    restart: unless-stopped
    env_file: .env
    ports:
      - "${WEBHOOK_PORT:-8282}:${WEBHOOK_PORT:-8282}" # webhook listener
