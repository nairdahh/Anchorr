services:
  jellycorrd:
    image: nairdah/jellycorrd:1.0.0
    container_name: jellycorrd
    restart: unless-stopped
    env_file: .env
    ports:
      - "${WEBHOOK_PORT:-8282}:8282" # webhook listener
